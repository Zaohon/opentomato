# Tool: get_station_forecast_24h

Purpose: Fetch next-24-hour forecast for one station, including predicted PV power and load power.

Execution: GET `/api/v1/forecast/stations/{station_id}` from forecast backend (default station_id=1).

