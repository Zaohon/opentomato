apiVersion: v1
kind: Service
metadata:
  name: {{ include "hems-agent-backend.fullname" . }}
  labels:
    {{- include "hems-agent-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "hems-agent-backend.labels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
